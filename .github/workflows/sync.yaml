name: Sync Fork

on:
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: tgymnich/fork-sync@v1.5
        with:
          owner: javiyt
          base: main
          head: main
          merge_method: squash
